<?php

use Drupal\node\NodeInterface;
/**
 * Implements hook_node_update_index().
 */
function ja_search_node_update_index(NodeInterface $node): ?string {
  if ($node->isPromoted()) {
    return 'promocionado';
  }

  return NULL;
}